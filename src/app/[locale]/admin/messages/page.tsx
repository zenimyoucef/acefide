import { prisma } from "@/lib/prisma";
export default async function MessagesPage() { const items = await prisma.contactMessage.findMany({ orderBy: { createdAt: "desc" }, take: 200 }); return <AdminTable title="Contact messages" headers={["Name","Email","Subject","Date"]} rows={items.map(x => [x.name,x.email,x.subject,x.createdAt.toLocaleDateString()])}/>; }
function AdminTable({title,headers,rows}:{title:string;headers:string[];rows:string[][]}) { return <div><h1 className="mb-6 text-2xl font-bold">{title}</h1><div className="overflow-x-auto rounded-xl border bg-white"><table className="w-full min-w-[700px] text-sm"><thead className="bg-muted"><tr>{headers.map(h=><th key={h} className="px-4 py-3 text-start">{h}</th>)}</tr></thead><tbody className="divide-y">{rows.map((r,i)=><tr key={i}>{r.map((c,j)=><td key={j} className="px-4 py-4">{c}</td>)}</tr>)}{!rows.length&&<tr><td colSpan={headers.length} className="p-10 text-center text-muted-foreground">No submissions yet.</td></tr>}</tbody></table></div></div> }
