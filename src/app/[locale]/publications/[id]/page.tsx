import { notFound } from "next/navigation";
import { prisma } from "@/lib/prisma";
export default async function Publication({params}:{params:Promise<{locale:string;id:string}>}){const {locale,id}=await params;const x=await prisma.publication.findFirst({where:{published:true,OR:[{id},{slug:id}]}}).catch(()=>null);if(!x)notFound();const title=locale==="ar"?x.titleAr:locale==="fr"?x.titleFr:x.titleEn;const summary=locale==="ar"?x.summaryAr:locale==="fr"?x.summaryFr:x.summaryEn;return <article dir={locale==="ar"?"rtl":"ltr"}><header className="bg-[#0b1f33] py-16 text-white"><div className="container-content max-w-4xl"><h1 className="text-4xl font-bold">{title}</h1></div></header><div className="container-content max-w-4xl py-16"><p className="text-lg leading-9 text-muted-foreground">{summary}</p>{x.pdfUrl&&<a href={x.pdfUrl} target="_blank" rel="noopener noreferrer" className="mt-8 inline-block rounded-lg bg-primary px-5 py-3 font-bold text-white">Download PDF</a>}</div></article>}
